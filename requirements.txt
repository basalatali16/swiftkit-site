# For running/testing the app on a desktop (not used for the Android build -
# buildozer.spec controls what actually gets bundled into the APK).
kivy==2.3.1
plyer
cryptography

# Optional: enables mic/speaker testing on desktop. Has no python-for-android
# recipe, so it is intentionally left out of buildozer.spec's requirements -
# on Android, call audio goes through pyjnius (AudioRecord/AudioTrack) instead.
pyaudio; sys_platform != "linux"
